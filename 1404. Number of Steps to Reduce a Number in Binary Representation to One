class Solution:
    def numSteps(self, s: str) -> int:
        decimal=int(s,2)
        temp=decimal
        steps=0
        while temp!=1:
            if(temp%2==0):
                temp//=2 
            else:
                temp=temp+1 
            steps+=1
        return steps
        
