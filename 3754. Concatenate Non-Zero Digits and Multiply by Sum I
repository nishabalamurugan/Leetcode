class Solution:
    def sumAndMultiply(self, n: int) -> int:
        nzd=[i for i in str(n) if i!='0']
        if(len(nzd)==0):
            return 0
        nzds=''.join(nzd)
        s=sum([int(i) for i in nzd])
        return s*int(nzds)

        
