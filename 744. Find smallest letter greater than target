class Solution:
    def nextGreatestLetter(self, letters: List[str], target: str) -> str:
        temp=letters[0]
        for letter in letters:
            if letter>target:
                return letter 
        return temp
                
        
