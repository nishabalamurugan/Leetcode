class Solution:
    def getMinDistance(self, nums: List[int], target: int, start: int) -> int:
        res=100000000 
        for i in range(len(nums)):
            if(nums[i]==target):
                temp=abs(i-start)
                if(temp<res):
                    res=temp
        return res
        
