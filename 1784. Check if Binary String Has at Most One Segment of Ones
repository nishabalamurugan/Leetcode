class Solution:
    def checkOnesSegment(self, s: str) -> bool:
        zero_found=False
        for i in range(len(s)):
            if(s[i]=='1'):
                if zero_found:
                    return False
            else:
                zero_found=True
        return True
