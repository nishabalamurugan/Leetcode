class Solution:
    def arrayRankTransform(self, arr: List[int]) -> List[int]:
        arr1=sorted(arr)
        dic={}
        rank=1 
        for i in arr1:
            if i not in dic:
                dic[i]=rank
                rank+=1 
            #print(dic)
        return [dic[i] for i in arr]
        
